import { Icon as DesignIcon } from "../../design/icons";

/**
 * @param {{name: string, size?: number, color?: string, strokeWidth?: number, style?: object}} props
 */
const Icon = (props) => <DesignIcon {...props} />;

export default Icon;
